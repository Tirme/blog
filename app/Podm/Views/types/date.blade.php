<div class="input-field">
    <input
        type="date"
        class="datepicker"
        name="{{$name}}"
        value="{{$value}}"
        placeholder="{{$placeholder}}"
        {{$required}}
        {{$editable}}
        />
    <label>{{ $label }}</label>
</div>